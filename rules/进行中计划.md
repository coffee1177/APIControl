# 进行中计划规范

1. 已确认并开始制作的需求，计划文件放入 `plan/<版本>/doing`。
2. 文件名使用“需求名+状态数字.md”，状态数字从 `0` 开始，例如 `01-项目构造0.md`。
3. 每完成一个可独立检查的小阶段，推进到下一个状态数字，并记录本阶段完成内容和下一步内容。
4. 同一需求只保留一个当前进行中的状态文件，旧状态文件保留作为过程记录。
5. 通过用户验收后，将最终结果写入对应版本的 `done.md` 和需求索引。
