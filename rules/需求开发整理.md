# 需求开发整理规范

1. 新需求先写入 `plan/<版本>/will`，一个需求一个文件，文件名尽量使用中文。
2. 开发前先确认需求范围；未确认的内容标记为“待确认”，不自行实现。
3. 需求文件至少写清目标、功能范围、明确不做和验收标准。
4. 开发过程中需求有变化时，先更新需求文档，再修改代码。
5. 不增加需求范围之外的功能，不进行无关重构。
6. 功能经用户验收后，再更新对应版本的 `done.md` 和需求索引。
